spring:
  config:
    activate:
      on-profile: dev

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.ecommerce.inventory: DEBUG
    org.hibernate.SQL: DEBUG